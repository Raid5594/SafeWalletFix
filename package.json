{
  "scripts": {
    "dev": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run start\"",
    "build": "concurrently \"cd backend && npm run start\" \"cd frontend && (npm run build && serve -s build -l 3000)\"",
    "contractsCompile": "cd contracts && truffle compile",
    "contractsDeploy": "cd contracts && truffle migrate"
  },
  "dependencies": {
    "concurrently": "^5.0.0",
    "react-router-dom": "^5.1.2"
  },
  "devDependencies": {
    "@babel/plugin-proposal-export-default-from": "^7.5.2"
  }
}
